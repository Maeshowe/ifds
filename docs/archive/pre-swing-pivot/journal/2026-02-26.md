# Journal — 2026-02-26

## Session összefoglaló

### MMS — Market Microstructure Scorer (BC17 prep)

Teljes task befejezve 4 commit, 4 review-körben.

| Lépés | Commit | Tesztek | Leírás |
|-------|--------|---------|--------|
| 1 | `5842615` | 861 | OBSIDIAN → MMS rename (15 fájl, 3 git mv, zero logic change) |
| 2 | `9c5df41` | 865 | venue_entropy — Shannon entropy a DP venue eloszlásból |
| 3 | `1609134` | 873 | iv_skew — ATM put IV − call IV a Polygon options-ból |
| 4 | `de7feaf` | 880 | 6-feature weights aktiválása (0.25/0.25/0.15/0.15/0.10/0.10) |

**19 új teszt:** 4 VenueMix + 6 IVSkew + 2 ZScores + 7 FeatureWeights. Mind zöld.

### Amit elvégeztünk

**Átnevezés (Lépés 1):**
- `phase5_obsidian.py` → `phase5_mms.py`, `obsidian_store.py` → `mms_store.py`
- `ObsidianStore` → `MMSStore`, `ObsidianAnalysis` → `MMSAnalysis`
- Összes `obsidian_*` config kulcs → `mms_*`
- `state/obsidian/` → `state/mms/` — 576 JSON fájl, baseline sértetlen
- CLAUDE.md, PARAMETERS.md, PIPELINE_LOGIC.md frissítve

**Venue Mix (Lépés 2):**
- `_aggregate_dp_records` — Shannon entrópia a `market_center` mező eloszlásából
- `FlowAnalysis.venue_entropy` mező hozzáadva
- Phase 4 átadja, MMS store-ba kerül

**IV Skew (Lépés 3):**
- `_compute_iv_skew()` helper — ATM put IV − call IV (5% sáv)
- Polygon options snapshot-ból, már meglévő adat
- MMS store-ba kerül

**Súlyok (Lépés 4):**
- 4 aktív feature-ről 6-ra bővült a scoring
- `dark_share=0.25, gex=0.25, venue_entropy=0.15, block_intensity=0.15, iv_rank=0.10, iv_skew=0.10`
- `TestFeatureWeights` (7 teszt) — config-paraméterezhetőség védve BC18+ hangoláshoz

### Állapot

- Pipeline viselkedése változatlan — az új feature-ök gyűlnek, de z-score csak ~márc végtől lesz
- 576 ticker baseline folytatódik az átnevezés után is (state/ fájlok megmaradtak)
- A 21 napos minimum periódushoz venue_entropy és iv_skew is szükséges lesz — ezért volt fontos most kezdeni

### Nyitott taskok (változatlan prioritás)

**🔴 CRITICAL — CC implementálja:**
1. `phase1_regime.py:203` — asyncio.gather `return_exceptions=True`
2. `eod_report.py:216-264` — idempotency guard
3. `earnings_exclusion_days` 5→7 — PARAMETERS.md + PIPELINE_LOGIC.md
4. `submit_orders.py:211-215` — circuit breaker halt + `--override-circuit-breaker` flag
5. `close_positions.py` + `nuke.py` — MOC/MKT order split >500

**🟡 BC17 előtt:**
6. `phase6_sizing.py` — `dataclasses.replace()` (MMS field drop megelőzés)
7. `validator.py` — MMS regime multiplier keys validálás
8. Phase 2/4/6 atomic file write-ok
9. `deploy_daily.sh` — pytest pre-flight + flock + Telegram alert + state backup
10. `test_base_client.py` + `test_async_base_client.py` — API retry tesztek

---

*Paper trading: Day 7/21 — cum P&L: +$328.65 (+0.33%)*
