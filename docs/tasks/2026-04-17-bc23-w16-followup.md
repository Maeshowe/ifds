# BC23 W16 Follow-up — Weekly Metrics Triggered Adjustments

**Status:** DONE
**Updated:** 2026-04-17
**Priority:** P1 — a heti metrika konkrét trigger pontokat adott
**Effort:** ~3-4h CC
**Depends on:** BC23 Phase 1-4 deploy (DONE, commit 0b905e6)
**Ref:** docs/analysis/weekly/2026-W16.md, docs/analysis/weekly/2026-W16-analysis.md

---

## Kontextus

A BC23 első teljes hete (ápr 13-17) lezárult. Abszolút számokban kiemelkedő eredmény (+$1,720.70 gross), de a mérés három problémát mutat:

1. **TP1 hit rate: 0/18 (0%)** — a BC23 plan kockázat-mitigációs pontja szerint "Ha a TP1 hit rate <5% → TP1 csökkentés 1.25×ATR-re". Ez most egyértelműen triggerelődött.

2. **Score→P&L korreláció: r = -0.414** — negatív korreláció perzisztál. Az inverz quintilis minta nem szűnt meg a BC23-mal. A scoring_validation újrafuttatását eredetileg május 2-re terveztük, de a negatív jel most itt van, nem várunk.

3. **Excess return -2.73% vs SPY bullish héten** — a rendszer alulteljesít bullish piacon. Ez nem közvetlen kódváltozás, de méréshez kell felkészíteni: a jövő hét (W17) makró kontextusa kritikus.

---

## Változások — 3 pont

### 1. TP1 csökkentés: 1.5×ATR → 1.25×ATR

**Trigger:** 0/18 TP1 hit rate (0%) — a BC23 plan rollback kritériuma.

**Fájl:** `src/ifds/config/defaults.py` → CORE

```python
# RÉGI (BC23):
"tp1_atr_multiple": 1.5,    # 1:1 R:R
# ÚJ (W17):
"tp1_atr_multiple": 1.25,   # tightened after 0% hit rate in W16
```

**Indoklás:**
- SL marad 1.5×ATR → R:R = 0.83:1 (kicsit R alatt, de reálisabb)
- Ha a jövő hét TP1 hit rate is <5% → további csökkentés 1.0×ATR-re (vagy TP1 teljes eltávolítása, csak trailing)
- Ne változtassunk meg semmi mást egyszerre — ez az egyetlen paraméter

**Tesztek:**
- Meglévő TP1 tesztek frissítése 1.25×ATR-re
- `test_tp1_ratio_to_sl` — R:R ratio ellenőrzés (0.83:1)

**Commit:**
```
fix(exit): tighten TP1 to 1.25×ATR after 0% hit rate in W16

Week 16 (Apr 13-17) weekly metrics showed 0/18 TP1 hits at 1.5×ATR.
Per BC23 rollback criteria ("if TP1 hit rate <5% → reduce to 1.25×ATR"),
tightening TP1 to allow the bracket rewards to trigger more frequently.

Next checkpoint: W17 metrics. If TP1 hit rate still <5% → consider
removing fixed TP1 and using trailing-only on Bracket B.
```

---

### 2. Scoring validáció újrafuttatás — BC23 live adatokkal

**Trigger:** r=-0.414 negatív korreláció a héten. Eredetileg 2 hét múlva terveztük (május 2), de a szignál most itt van.

**Fájl:** `scripts/analysis/scoring_validation.py` — létező script, bemenet módosítás.

**Feladat:**

1. Script módosítás: a `scoring_validation.py` filterezze az adatokat dátum szerint, csak a BC23 post-deploy (2026-04-13+) adatokra fusson
2. Adatforrás: `state/daily_metrics/2026-04-13.json` ... `2026-04-17.json` (5 nap, 18 trade)
3. Output: `docs/analysis/scoring-validation-bc23-w16.md`

**Mit várunk látni:**

- **Score→P&L korreláció (BC23):** r=? (a W16 heti metrika szerint -0.414, de a teljes scoring_validation többet mond)
- **Quintilis elemzés:** Q1-Q5 spread. A BC23 előtt Q5 volt a legrosszabb. Mi a minta most?
- **Flow/Funda/Tech al-korrelációk:** melyik komponens javult a BC23-mal, melyik rontott
- **Score range kihasználása:** a BC23 után csak 90-95 tartomány van (threshold 85 + scoring shift). Ebben a szűk sávban van-e differenciálás?

**Tesztek:** nincs — ez elemzési feladat.

**Commit:**
```
analysis: rerun scoring validation on BC23 post-deploy data (W16)

5 days, 18 trades, first full BC23 week.
Output: docs/analysis/scoring-validation-bc23-w16.md

Accelerated from planned May 2 after weekly metrics showed
persistent negative correlation (r=-0.414).
```

---

### 3. Flow al-komponens dekompozíció — BEFEJEZÉS

**Trigger:** Ez a BC23 Phase 4 task része volt (`2026-04-11-bc23-phase4-simplification.md`), de még nem készült el. Most extra fontos: ha a scoring továbbra is negatív korrelációt mutat, a flow dekompozíció megmondja **melyik** al-komponens okozza.

**Fájl:** `scripts/analysis/flow_decomposition.py` (ÚJ)

**Adatforrás:**

- `state/phase4_snapshots/YYYY-MM-DD.json.gz` — tartalmazza a flow al-komponenseket (vagy csak az aggregált rvol_score-t, ellenőrizni)
- `scripts/paper_trading/logs/cumulative_pnl.json` — napi P&L
- `state/daily_metrics/*.json` — BC23 adatok

**Ha a Phase 4 snapshot nem tartalmazza az al-komponenseket:**

Először bővíteni kell a snapshot mentést — ez a tényleges CC munka:

```python
# phase4_stocks.py — snapshot mentésnél:
snapshot_data = {
    # ... meglévő mezők ...
    "flow_components": {
        "rvol_score": flow.rvol_score_raw,      # pure RVOL, squat nélkül
        "squat_bar_bonus": flow.squat_bar_bonus,
        "dp_pct_score": flow.dp_pct_score,
        "pcr_score": flow.pcr_score,
        "otm_score": flow.otm_score,
        "block_trade_score": flow.block_trade_score,
        "buy_pressure_score": flow.buy_pressure_score,
    }
}
```

Megjegyzés: ehhez a `FlowAnalysis` model-be bontani kell az `rvol_score`-t `rvol_score_raw + összetevők` formára. Jelenleg az összeg egyetlen mezőben van.

**Elemzés tartalma:**

Minden flow al-komponensre:
- Pearson és Spearman korreláció a napi P&L-lel
- Quintilis elemzés (top/bottom 20%)
- Market cap szegmentálás: <$10B vs $10-50B vs >$50B (a BC23 plan megjegyzése: "flow jelek small/mid cap-re erősebbek")

**Kimenet:** `docs/analysis/flow-decomposition.md`

**Várt felhasználás:**

- Ha `dp_pct_score` erősen prediktív → súly emelés
- Ha `rvol_score` nem prediktív → kivenni a flow-ból
- Ha `pcr_score` negatív → kikapcsolni
- Az elemzés jövő heti (W17) döntéseket információl

**Tesztek:**
- `test_flow_snapshot_has_components` — a snapshot tartalmazza a 7 al-komponenst
- Elemzés script: nincs unit teszt (egyszeri futtatás)

**Commit (2 commit):**
```
feat(phase4): decompose flow_score into component snapshot fields

FlowAnalysis model gets individual component scores alongside
the aggregated rvol_score. Phase 4 snapshot saves all 7 components
for later predictiveness analysis.

Needed for flow decomposition analysis (BC23 Phase 4 task).
```

```
analysis: flow component decomposition — per-component P&L correlation

Output: docs/analysis/flow-decomposition.md
Breaks down the 7 flow sub-components by predictive strength.
Market cap segmentation included (BC23 plan note on small/mid cap).
```

---

## Nem változik

- Scoring súlyok (flow 0.60, funda 0.10, tech 0.30) — **egyelőre marad**. Változás csak ha a `scoring-validation-bc23-w16` ezt indokolja.
- Dynamic threshold (85) — marad. A W16-ban 0/5 nulla-pozíció nap, 0/5 alacsony-pozíció nap → jól kalibrált.
- Max positions (5), risk per trade (0.7%) — marad
- Submit timing (16:15) — marad, működik
- TP2 (2.0×ATR) — marad, bár 0 hit rate. A TP2 a bracket B védelem, nem feltétlenül kell beütnie
- Multiplier chain (3 aktív) — marad
- MMS disabled, VWAP REJECT only, Call Wall TP1 off — marad

## Végrehajtási sorrend

1. **Ma (péntek) vagy szombat délelőtt**: #1 TP1 csökkentés — 5 perc + teszt + commit
2. **Szombat vagy vasárnap**: #2 scoring validáció újrafuttatás — csak a script módosítás, futtatás, markdown output. ~1 óra
3. **Vasárnap**: #3 flow dekompozíció — ez a legnagyobb munka (~2 óra), mert a snapshot bővítés kell először. **Megjegyzés: ha a snapshot bővítés bonyolult, elhalasztható W18-ra — a #1 és #2 kritikusabbak.**

Hétfő 16:15-kor az új TP1 (1.25×ATR) aktív. A jövő heti (W17) metrika megmutatja:
- TP1 hit rate emelkedett-e >5%-ra
- Score korreláció javult/rosszabb lett-e
- Bullish vagy bearish volt-e a piac — az excess return kontextusához

## Siker kritériumok (W17 után)

| Metrika | W16 | Cél W17 | Akció ha nem |
|---------|-----|---------|--------------|
| TP1 hit rate | 0% | >5% | TP1 további csökkentés 1.0×ATR-re |
| Score→P&L korreláció | -0.414 | >-0.2 (közeledés 0-hoz) | Flow súly újratervezés a dekompozíció alapján |
| Excess vs SPY | -2.73% | nem tovább romlik | Bearish hétben pozitív excess → edge jel |
| Nyereséges napok | 4/5 | ≥3/5 | Ha <3/5 → a scoring átgondolás |
